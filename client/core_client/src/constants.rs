pub use nanopyrs::constants::*;

pub const ONE_SECOND: u64 = 1;
pub const ONE_MINUTE: u64 = ONE_SECOND * 60;
pub const ONE_HOUR: u64 = ONE_MINUTE * 60;
pub const ONE_DAY: u64 = ONE_HOUR * 24;
pub const ONE_WEEK: u64 = ONE_DAY * 7;
